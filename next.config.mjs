/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add image domains here if you load project screenshots from external URLs:
  // images: { remotePatterns: [{ protocol: "https", hostname: "..." }] },
};

export default nextConfig;
